apiVersion: apps/v1
kind: Deployment
metadata:
  name: rsvp-db
spec: 
  replicas: 1
  selector: 
   matchLabels: 
    appdb: rsvpdb2
  template:
    metadata:
      labels:
        appdbs: rsvpdb2
    spec: 
      containers:
      - name: rsvpd-db2
        image: mongo:3.3
        env:
        - name: MONGODB_DATABASE
          value: rsvpdata
        ports:
        - containerPort: 27017
